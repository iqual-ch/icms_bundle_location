# ICMS Recipe: Location

> A content type and layout for publishing locations.

### Useful commands
- Install recipe:
  ```bash
  composer require iqual/icms_bundle_location && drush recipe ../packages/icms_bundle_location
  ```
